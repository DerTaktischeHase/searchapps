## searchapps

`searchapps` ist ein kleines CLI-Tool für Linux, das alle ausführbaren Programme in deinem `PATH` auflistet und nach Namen durchsuchen kann (case-insensitive, Teilstrings).

## Installation

Installieren mit `curl`:
```sh
curl -fsSL https://raw.githubusercontent.com/DerTaktischeHase/searchapps/main/install.sh | sh